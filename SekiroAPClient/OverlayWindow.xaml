<Window x:Class="SekiroAPClient.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SekiroAPClient"
        xmlns:cl="clr-namespace:SekiroAPClient.Classes"
        xmlns:cnt="clr-namespace:SekiroAPClient.Controls"
        xmlns:vm="clr-namespace:SekiroAPClient.ViewModels"
        xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        ShowInTaskbar="False"        
        AllowsTransparency="True"
        Background="Transparent"
        WindowStyle="None"
        ResizeMode="CanResizeWithGrip"
        x:Name="window"
        Title="BoardWindow"
        d:DataContext="{d:DesignInstance Type=vm:RoomViewModel, IsDesignTimeCreatable=False}"
        Height="600"
        Width="500">
    <Window.Resources>

    </Window.Resources>
    <Grid Background="#01000000" MouseDown="Grid_MouseDown">
        <Viewbox Stretch="Uniform" x:Name="mainViewBox" >
            <Grid Background="#01000000" MinHeight="600" MinWidth="550">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="50" />
                </Grid.RowDefinitions>
                <Grid Width="550">
                    <ItemsControl ItemsSource="{Binding KeyItemTracker.KeyItems}"
                                  Margin="0,40,0,0"
                                  ScrollViewer.CanContentScroll="False"
                                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                  HorizontalContentAlignment="Center">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal"
                                           HorizontalAlignment="Center" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Height="150"
                                      Width="100"
                                      Margin="0,0,10,10">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="1*" />
                                        <RowDefinition Height="50" />
                                    </Grid.RowDefinitions>
                                    <Image Source="{Binding CheckedImageSource}"
                                           Stretch="Uniform"
                                           Visibility="{Binding IsChecked, Converter={StaticResource BoolToVisConverter}}"
                                           Margin="5" />
                                    <Image Source="{Binding UnCheckedImageSource}"
                                           Stretch="Uniform"
                                           Visibility="{Binding IsChecked, Converter={StaticResource NotBoolToVisibilityConverter}}"
                                           Margin="5" />
                                    <Border Background="{DynamicResource AccentBrush}"
                                            Height="25"
                                            Width="25"
                                            CornerRadius="13"
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Top"
                                            Margin="5"
                                            BorderBrush="{DynamicResource PrimaryTextBrush}"
                                            BorderThickness="2"
                                            Visibility="{Binding IsChecked, Converter={StaticResource BoolToVisConverter}}">
                                        <TextBlock FontFamily="{StaticResource FluentIcons}"
                                                   Text="&#xE65F;"
                                                   Foreground="{DynamicResource PrimaryTextBrush}"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center" />
                                    </Border>
                                    <TextBlock Text="{Binding Name}"
                                               Style="{StaticResource FlatTextBlockStyle}"
                                               FontSize="12"
                                               HorizontalAlignment="Center"
                                               Grid.Row="1"
                                               FontFamily="{StaticResource LexendDeca}"
                                               TextWrapping="Wrap"
                                               TextTrimming="CharacterEllipsis"
                                               TextAlignment="Center" />
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
                <Grid Grid.Row="1">
                    <StackPanel HorizontalAlignment="Left"
                                VerticalAlignment="Center"                               
                                Orientation="Horizontal"
                                Margin="30,0,0,0">
                        <TextBlock Text="Death Counter: "
                                   Style="{StaticResource HeaderTextBlockBlockStyle}" />
                        <TextBlock Text="{Binding State.DeathCounter}"
                                   Style="{StaticResource HeaderTextBlockBlockStyle}"
                                   TextTrimming="CharacterEllipsis"
                                   TextWrapping="NoWrap" />
                    </StackPanel>
                </Grid>
            </Grid>
        </Viewbox>
        <Grid HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Style="{StaticResource IconButtonStyle}" HorizontalAlignment="Right" VerticalAlignment="Center"
                Content="&#xE10A;" Click="Button_Click"/>
        </Grid>
    </Grid>
</Window>
