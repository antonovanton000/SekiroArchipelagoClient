<Window x:Class="AppUpdater.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AppUpdater"
        mc:Ignorable="d"
        Loaded="Window_Loaded"
        WindowStyle="None"
        AllowsTransparency="True"
        Title="MainWindow" Height="250" Width="350"
        WindowStartupLocation="CenterScreen"
        Background="Transparent">
    <Border CornerRadius="8" BorderBrush="{StaticResource BorderColorBrush}" BorderThickness="1">
        <Border.Background>
            <ImageBrush ImageSource="/Images/grass.png"/>
        </Border.Background>
        <Grid>
            <TextBlock Text="" Foreground="White" FontSize="22" FontFamily="Segoe UI Light" 
                       HorizontalAlignment="Center" TextWrapping="Wrap" Margin="10,0" TextAlignment="Center"
                       VerticalAlignment="Center" x:Name="tbl"/>
            <ProgressBar IsIndeterminate="True" HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="20,60,20,0"
                         BorderThickness="0" Background="Transparent" Foreground="{StaticResource AccentBrush}" Height="3" x:Name="progress"/>
            <TextBlock Text="" Foreground="White" FontSize="15" FontFamily="Segoe UI Light" 
                       HorizontalAlignment="Center" Margin="0,100,0,0"
                       VerticalAlignment="Center" x:Name="progressText"/>
            <Button Content="Cancel" Style="{StaticResource FlatTextButtonStyleNoArrow}" VerticalAlignment="Bottom" Margin="0,0,0,20"
                    Click="Cancel_Click" HorizontalAlignment="Center" Width="100"/>
        </Grid>
    </Border>
</Window>
